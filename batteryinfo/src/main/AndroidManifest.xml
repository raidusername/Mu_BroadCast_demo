<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.demo.zrodo.batteryinfo">

    <application
        android:name=".My_app"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        /**
        android:screenOrientation="portrait"//只针对当前的activity   只支持竖屏portrait     横屏landscape
        **/
        <activity android:name=".MainActivity"


            android:configChanges="orientation|screenSize|keyboardHidden"> //自己切


            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Main2Activity"></activity>
        <service android:name=".TestIntentServices">

        </service>
    </application>

</manifest>